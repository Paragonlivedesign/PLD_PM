rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PLD_PM: authenticated users only (anonymous auth OK for dev/emulator).
    // Tighten with custom claims / tenant paths when multi-tenant backend lands.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
